active proctype pid0 (){
	assert(_pid == 0);
}

active proctype pid1 (){
	assert(_pid == 1);
}

active proctype pid2 (){
	assert(_pid == 2);
}

active proctype pid3 (){
	assert(_pid == 3);
}

